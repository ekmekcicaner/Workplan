@if (!string.IsNullOrWhiteSpace(Plan.LatestRejectionReason))
{
    <WpNotice Tone="WpTone.Danger" Title="Reddedildi" Text="@Plan.LatestRejectionReason">
        <span class="text-xs font-medium uppercase tracking-wide text-danger">Tekrar kontrol et</span>
    </WpNotice>
}

@code {
    [Parameter, EditorRequired] public DailyPlanListItemDto Plan { get; set; } = default!;
}
