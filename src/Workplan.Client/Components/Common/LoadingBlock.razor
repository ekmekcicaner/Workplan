<WpLoadingState Loading="Loading" ErrorText="@ErrorText" Text="@Text" />

@code {
    [Parameter] public bool Loading { get; set; }
    [Parameter] public string? ErrorText { get; set; }
    [Parameter] public string Text { get; set; } = "Yükleniyor...";
}
